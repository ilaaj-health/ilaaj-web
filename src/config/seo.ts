// Google Search Console verification.
// Get the code from Search Console → Settings → Ownership verification → "HTML tag"
// and paste ONLY the content value below (e.g. 'abc123...').
// Prefer DNS TXT verification for robustness across subdomains — if you use DNS,
// leave this blank so no empty/placeholder tag ships to production.
export const GSC_VERIFICATION = '';
